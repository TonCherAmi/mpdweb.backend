name: CI Test

on:
  push: []

jobs:
  test-darwin:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v3
      - run: cargo test

  test-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: cargo test
